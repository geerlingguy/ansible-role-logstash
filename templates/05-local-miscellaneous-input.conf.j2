#{{ ansible_managed }}
input {
  file {
    path => "/var/log/supervisor/*.log"
  }

  file {
    path => "/var/log/zabbix-agent/*.log"
  }

  file {
    path => "/var/log/zabbix/*.log"
  }

  file {
    path => "/var/log/tomcat7/*.log"
    exclude => ["*.gz"]
  }

  file {
    path => "/var/log/rabbitmq/*.log"
    exclude => ["*.gz"]
  }

  file {
    path => "/var/log/postgresql/*.log"
    exclude => ["*.gz"]
  }

  file {
    path => "/var/log/mysql/*.log"
  }

  file {
    path => "/var/log/mon/*.log"
  }

  file {
    path => "/var/log/mail.log"
  }

  file {
    path => "/var/log/upstart/*.log"
  }

  file {
    path => "/var/log/docker*.log"
  }

  file {
    path => "{{ kgr_home }}/log/*.log"
  }

  file {
    path => "{{ kgr_home }}/dist/log/*.log"
  }

  file {
    path => "{{ kgr_home }}/dist/3rdparty/J2EE/tomcat_dist/logs/*.log"
  }

  file {
    path => "{{ kgr_home }}/data/J2EE/TomcatConfigs/logs/*.log"
  }

}
